# SOC Trainer — .gitignore

# Never commit API keys (they're session-only in the app, but just in case)
.env
.env.local
*.env

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor directories
.vscode/
.idea/
*.swp
*.swo

# Node (if someone adds a dev server)
node_modules/
package-lock.json

# Build artifacts
dist/
build/
